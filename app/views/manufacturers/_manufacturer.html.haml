%tr
  %td= manufacturer.name
  %td= link_to t("actions.edit"), edit_manufacturer_path(manufacturer), :class => "btn btn-primary"
  %td= link_to t("actions.delete"), manufacturer, confirm: 'Are you sure?', method: :delete, :class => "btn btn-danger"
